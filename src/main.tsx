import React from 'react'
import ReactDOM from 'react-dom/client'
import Cards_holder from './Cards_holder/Cards_holder'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <Cards_holder />
  </React.StrictMode>,
)